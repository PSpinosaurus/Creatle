@page "/answer/index"
@using BusinessLayer;
@using ServiceLayer;
@inject AnswerManager answerManager;


<h3>Index</h3>
<a href="answer/create">Create</a>

<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (Answer answer in model)
        {
            <tr>
                <td>
                    @answer.CategoryValue
                </td>
                <td>
                    <a href="answer/update/@answer.GameId/@answer.CategoryId/@answer.Date.Year/@answer.Date.Month/@answer.Date.Day">Edit</a> |
                    <a href="answer/details/@answer.GameId/@answer.CategoryId/@answer.Date.Year/@answer.Date.Month/@answer.Date.Day">Details</a> |
                    <a href="answer/delete/@answer.GameId/@answer.CategoryId/@answer.Date.Year/@answer.Date.Month/@answer.Date.Day">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@code
{
    private IEnumerable<Answer> model = new List<Answer>();

    protected override async Task OnInitializedAsync()
    {
        model = await answerManager.ReadAllAsync(true);

        await base.OnInitializedAsync();
    }


}
